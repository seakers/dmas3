package modules.components;

public class ThermalSubsystem extends AbstractSubsystem{
    public ThermalSubsystem(String name, double power, double mass, double x_dim, double y_dim, double z_dim) {
        super(name, power, mass, x_dim, y_dim, z_dim);
    }
}
