package modules.components;

public class ElectronicPowerSubsystem extends AbstractSubsystem{
    public ElectronicPowerSubsystem(String name, double power, double mass, double x_dim, double y_dim, double z_dim) {
        super(name, power, mass, x_dim, y_dim, z_dim);
    }
}
