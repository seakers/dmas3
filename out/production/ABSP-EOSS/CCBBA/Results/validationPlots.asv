function [] = validationPlots()
    clc; clear all;
    
    % Obtain results folders in results directory
    results = readData();               %- "results" struct now contains data from all validation experiments
    
    
    % Create plots for all figures
    figureThree(results);
    
end

